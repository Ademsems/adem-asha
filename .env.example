# Resend API key — required for the contact form to send email.
# Without it the site still builds and runs; the form shows a fallback message.
RESEND_API_KEY=

# Optional: verified sender address once your domain is set up in Resend.
# Defaults to "Adem Asha Website <onboarding@resend.dev>" when unset.
CONTACT_FROM_EMAIL=
